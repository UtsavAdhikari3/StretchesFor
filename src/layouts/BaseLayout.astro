---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getLocaleFromPath, localeInfo, localePath, locales, switchLocalePath, t, type Locale } from '../i18n';

interface Props {
  title: string;
  description: string;
  canonicalPath?: string;
  image?: string;
  imageAlt?: string;
  imageWidth?: number;
  imageHeight?: number;
  imageType?: string;
  ogType?: 'website' | 'article';
  robots?: string;
  locale?: Locale;
}

const {
  title,
  description,
  canonicalPath = Astro.url.pathname,
  image = '/social-card.png',
  imageAlt = 'stretchesfor guided stretching and mobility tool',
  imageWidth = 1200,
  imageHeight = 630,
  imageType,
  ogType = 'website',
  robots = 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1',
  locale = getLocaleFromPath(Astro.url.pathname),
} = Astro.props;
const canonical = new URL(localePath(locale, canonicalPath), Astro.site);
const socialImage = new URL(image, Astro.site);
const socialImageType = imageType ?? (image.endsWith('.webp') ? 'image/webp' : image.endsWith('.svg') ? 'image/svg+xml' : 'image/png');
const fullTitle = title === 'stretchesfor' ? title : `${title} | stretchesfor`;
const translatedTitle = t(locale, fullTitle);
const translatedDescription = t(locale, description);
const translatedImageAlt = t(locale, imageAlt);
const alternatePath = (alternateLocale: Locale) => switchLocalePath(canonical.pathname, alternateLocale);
---

<!doctype html>
<html lang={localeInfo[locale].htmlLang} data-theme="light">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#f7f8fa" />
    <meta name="description" content={translatedDescription} />
    <meta name="robots" content={robots} />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonical} />
    {locales.map((alternateLocale) => <link rel="alternate" hreflang={localeInfo[alternateLocale].htmlLang} href={new URL(alternatePath(alternateLocale), Astro.site)} />)}
    <link rel="alternate" hreflang="x-default" href={new URL(alternatePath('en'), Astro.site)} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="stretchesfor" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content="stretchesfor" />
    <meta property="og:locale" content={localeInfo[locale].ogLocale} />
    {locales.filter((alternateLocale) => alternateLocale !== locale).map((alternateLocale) => <meta property="og:locale:alternate" content={localeInfo[alternateLocale].ogLocale} />)}
    <meta property="og:title" content={translatedTitle} />
    <meta property="og:description" content={translatedDescription} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta property="og:image:secure_url" content={socialImage} />
    <meta property="og:image:type" content={socialImageType} />
    <meta property="og:image:width" content={String(imageWidth)} />
    <meta property="og:image:height" content={String(imageHeight)} />
    <meta property="og:image:alt" content={translatedImageAlt} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={translatedTitle} />
    <meta name="twitter:description" content={translatedDescription} />
    <meta name="twitter:image" content={socialImage} />
    <meta name="twitter:image:alt" content={translatedImageAlt} />
    <title>{translatedTitle}</title>
    <script is:inline>
      (() => {
        const storageKey = 'stretchesfor-theme';
        let theme = 'light';

        try {
          const savedTheme = localStorage.getItem(storageKey);
          if (savedTheme === 'light' || savedTheme === 'dark') theme = savedTheme;
        } catch {
          // Storage can be unavailable in privacy-restricted browsing contexts.
        }

        document.documentElement.dataset.theme = theme;
        document.querySelector('meta[name="theme-color"]')?.setAttribute('content', theme === 'light' ? '#f7f8fa' : '#010102');
      })();
    </script>
    <slot name="head" />
  </head>
  <body class="text-ink antialiased">
    <a href="#main-content" class="fixed left-4 top-4 z-50 -translate-y-24 rounded-lg bg-brand px-4 py-3 font-semibold text-white transition-transform duration-200 focus:translate-y-0">{t(locale, 'Skip to main content')}</a>
    <Header locale={locale} pathname={Astro.url.pathname} search={Astro.url.search} />
    <main id="main-content"><slot /></main>
    <Footer locale={locale} />
  </body>
</html>
